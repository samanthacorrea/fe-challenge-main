import styled from 'styled-components'

const TextTransformCapitalize = styled.span`
  text-transform: capitalize;
  font-weight:700;
`;

const style = {
	TextTransformCapitalize,
};

export default style;